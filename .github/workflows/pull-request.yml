name: pull-request
on:
  pull_request:
    types: [opened, synchronize, reopened]
  merge_group:

jobs:
  code:
    uses: repobuddy/.github/.github/workflows/pnpm-verify.yml@v2
    with:
      os: '["ubuntu-latest"]'
